# Beszel

| | |
|--|--|
| URL (dev) | http://192.168.122.50:8090 |
| Login | email `dev-homelabnotifications@pm.me` |
| Secret | `BESZEL_ADMIN_PASSWORD` |
| Bootstrap | `utils/bootstrap/beszel.sh` |
| Auth API | `POST /api/collections/_superusers/auth-with-password` |

Hub superuser is upserted by `beszel superuser upsert` (distroless image — value stays on the container argv).

`BESZEL_KEY` is **issued by the hub**, not generated by `gen-app-credentials.sh`. Agent (`profile: beszel-agent`, host net `:45876`) stays off until that key is cached. System register has returned HTTP 400 on DEV — agent enablement is optional.
