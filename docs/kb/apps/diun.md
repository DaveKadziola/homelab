# Diun

| | |
|--|--|
| UI | none |
| Login | none |
| Config-as-code | yes |

Image-update watcher. Logs to stdout (Dozzle). Notification mailbox is the env identity (`homelab-notification`) once F10 wiring exists.
