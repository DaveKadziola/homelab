# Scan the git tree. Working-tree OPNsense XML is gitignored on purpose
# (see docs/kb/security-audit-2026-09-10.md S9) and must not fail CI.
title = "homelab"

[allowlist]
paths = [
  '''opnsense/backups/''',
  '''.*\.tfstate$''',
  '''compose/core/authelia/users_database\.yml$''',
]
